#!/usr/bin/env zsh

# Start tmux session if not already inside tmux
if [[ -z "${TMUX:-}" ]]; then
  tmux new-session -A -s raspberry-pi
fi

# Aliases for Raspberry Pi package names
alias bat='batcat'
alias fd='fdfind'


