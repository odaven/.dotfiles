[ -f "$HOME/.agents/config/zsh/zshrc" ] && source "$HOME/.agents/config/zsh/zshrc"
[ -f "$ZDOTDIR_LOCAL"/zshrc ] && source "$ZDOTDIR_LOCAL"/zshrc
[ -f "$ZDOTDIR_PRIVATE"/zshrc ] && source "$ZDOTDIR_PRIVATE"/zshrc
